import React from 'react';
import { NavBar } from './NavBar';
import { Loading } from './Loading';

export const Favorites = () => {
  return (
    <div>
      <Loading />
      Favorites
      <NavBar />
    </div>
  )
}
